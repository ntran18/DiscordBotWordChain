/**
 * Common UI strings shared across commands.
 */
const common = {
    genericError: "Có vấn đề xảy ra, vui lòng liên hệ chủ bot.",
    noPermission: "Bạn không có quyền dùng lệnh này.",
    ownerOnly: "Chỉ chủ bot mới có thể dùng lệnh này.",
    syncCooldown: "Lệnh này đang trong thời gian chờ. Vui lòng thử lại sau.",
};

module.exports = { common };
